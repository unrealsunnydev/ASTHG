{
	"$schema": "http://json-schema.org/draft-07/schema#",

	"title": "ASTHG Music Metadata",
	"type": "object",
	"properties": {
		"title": {
			"type": "string",
			"description": "The title of the music track."
		},

		"artist": {
			"type": "string",
			"description": "The artist of the music track."
		},

		"album": {
			"type": "string",
			"description": "The album of the music track."
		},

		"albumArtist": {
			"type": "string",
			"description": "The album artist of the music track."
		},

		"track": {
			"type": "integer",
			"description": "The track number of the music track."
		},

		"year": {
			"type": "integer",
			"description": "The year the music track was released."
		},

		"genre": { "type": "string" },

		"loop": {
			"type": "boolean",
			"description": "Whether the music track should loop.",
			"default": false

		},

		"loopStart": {
			"type": "integer",
			"description": "(OPTIONAL) The start time of the loop in samples.",
			"minimum": 0,
			"default": 0
		}
	},
	"dependencies": {
		"loopStart": {
			"properties": {
				"loop": { "const": true }
			},
			"required": ["loop"]
		}
	}
}
